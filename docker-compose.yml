version: '3'

services:
  web:
    build:
      dockerfile: ./Dockerfile
      context: ./
    volumes:
      - './imgs:/app/imgs'
      - './db:/app/db'
    ports:
      - '8501:8501'
  bot:
    build:
      dockerfile: ./Dockerfile1
      context: ./
    volumes:
      - './imgs:/app/imgs'
      - './db:/app/db'

volumes:
  config:
    driver: local
  images:
    driver: local